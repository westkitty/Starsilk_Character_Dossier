# Temporal Cartographer — Operational State

## Revision 2 — 2026-09-08 — Optimal synthesis validated

### Active branch

`feature/temporal-cartographer-optimal-2026-09-08`

Base at branch creation: `5a93d862ab3938550febfee7d55cdf8d8ec65853`
(`feature/temporal-cartographer-2026-09-07-2`, including the Drakken overwrite sequence).

Validated product source before this documentation-only revision:
`ed4dc65985d5e94a31c7379ede941931606be76d`.

Parent repository `main` baseline observed before work:
`5537f411354e3a3e83849f8c369cc628d4e5faaf`.

### Protected boundaries

- Do not merge automatically.
- Do not edit generated dossier `/docs` output.
- Cartographer JSON remains authoritative only for Cartographer-authored map state.
- Existing STARSiLK canon sources remain authoritative for lore/canon facts.
- Three.js objects are derived presentation state and never persistent authority.
- Orbital simulation time and historical/era time remain independent.
- Historical time remains hierarchical: galaxy → starfield → system → object.
- Main narrative remains `MAIN NARRATIVE — DATE UNSPECIFIED`; no numeric year may be invented.
- Unsourced coordinates remain `SCHEMATIC / NON-CANON POSITION` or `unknown`.
- Starsilk remains non-sentient.
- Blood Rings remain solid orbital bands, not Saturn-like rings or shields.
- Siege Wall default visualization remains missing light / stellar absence, not a literal wall.

### Implemented in this synthesis

- Branch B modular model/store/render/UI architecture retained as the base.
- Added `src/model/analyst.ts` as a pure analysis layer.
- Added ultimate temporal-authority tracing rather than only immediate-parent provenance.
- Added Palimpsest/composite-time detection and active local-pin inventory.
- Added historical TRACE / WHY explanation data.
- Added DELTA comparison of resolved historical states.
- Added optional field-level truth lattice (`EntityMeta.truth`).
- Added explicit `unknown` position certainty alongside locked/schematic.
- Added knowledge-gap detection and curated structured canon queries.
- Added independent accessible Analyst Mode workstation UI.
- Repaired system-collapse propagation so nested moons, Blood Rings, and structures under planets are destroyed with a collapsed system.
- Repaired Analyst Mode mounting so it lives inside the editor's open Shadow DOM rather than invisible light DOM.
- Added analyst regression tests for authority, composite time, Blood Ring creation, Aureal collapse, nested collapse propagation, truth states, gaps, and queries.
- Added Node test typings so the package's declared TypeScript check actually typechecks its existing Node-based test suite.
- Added `package-lock.json` generated by the clean validation runner and then transplanted by exact Git blob identity.

### Verified evidence

Disposable validation branch:
`validation/temporal-cartographer-optimal-ci-2026-09-08`

Disposable validation PR: `#62` (DO NOT MERGE).

#### Full browser validation

GitHub Actions run `34188041806` — **PASS**.

Verified on Ubuntu / Node 22:

- dependency installation succeeds;
- `npm test`: **30 tests passed, 0 failed**;
- `npm run typecheck`: pass;
- `npm run build`: pass;
- Vite production output created successfully;
- `git diff --check`: pass;
- headless Chromium launches with software WebGL and no page/console errors;
- Fallenstar Region local Year 3 override resolves correctly;
- TRACE reports Fallenstar Region as ultimate temporal authority;
- PALIMPSEST identifies the composite temporal view;
- First Blood Ring DELTA Year 0 → Year 3 reports `CREATED`;
- Aureal DELTA Year 121 → Year 170 reports star → black hole `TRANSFORMED` plus `COLLAPSE`;
- TRUTH lattice renders and accepts an explicit field classification;
- the field classification survives the real debounced IndexedDB save and full page reload;
- GAPS surfaces unknown knowledge state;
- QUERY `collapse-affected` returns Aureal-related effects;
- a second live mount in viewer mode has the viewer class, no Analyst workstation, and zero visible authoring controls.

Vite reports one non-fatal bundle-size warning: main JavaScript is approximately 646.94 kB (168.73 kB gzip), above its default 500 kB warning threshold. Treat this as later optimization/performance work, not a failed build.

#### Locked clean build

GitHub Actions run `34188237935` — **PASS**.

Using the committed `package-lock.json` and `npm ci` on Ubuntu / Node 22:

- exact dependency graph installation: pass;
- `npm test`: pass;
- `npm run typecheck`: pass;
- `npm run build`: pass;
- `git diff --check`: pass.

This upgrades dependency reproducibility from unverified semver resolution to a committed npm lockfile with a successful clean `npm ci` proof.

### Defects discovered and repaired during proof

1. **TypeScript validation configuration** — tests were included by `tsconfig.json` but Node typings were not declared. Repaired with `@types/node` plus strict-safe test typing.
2. **Analyst Shadow DOM boundary** — Analyst Mode was initially mounted in host light DOM while the editor owns an open Shadow DOM with no slot, making the Analyst UI invisible. Repaired by mounting Analyst inside the editor shadow boundary.
3. **Nested collapse propagation** — system collapse previously affected direct system children but could miss moons/Blood Rings/structures nested under planets. Repaired with ancestor-system resolution.

### Remaining evidence / later work

- Long-session Three.js lifecycle and memory recovery have not been profiled; use a matched performance/lifecycle audit before making sustained-performance claims.
- The current bundle-size warning is real but not yet tied to a measured user-path problem.
- Real mobile-device thermal behavior is unverified.
- Human aesthetic/usability acceptance remains a human review decision.

### Merge posture

**READY FOR HUMAN REVIEW, NOT AUTO-MERGE.**

The synthesis is built and strongly validated, but it remains one candidate among the user's intended Cartographer variants. Do not merge into Branch B or `main` until the multi-variant comparison is complete and the user explicitly chooses the final integration path.
