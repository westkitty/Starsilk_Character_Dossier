{
  "name": "@starsilk/temporal-cartographer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "STARSiLK Temporal Cartographer — isolated 3D galaxy / solar-system authoring tool with hierarchical historical time.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types --test src/tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/three": "^0.185.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
