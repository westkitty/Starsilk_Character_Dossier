# Offline Archive Edition — the giant embedded-media source file.
# This must never enter Git history (see README.md for why).
starsilk_character_dossier.html
archive/

# Raw hero video source (tools/add_hero_video_and_rebrand.py processes this
# into a hashed, audio-stripped copy under docs/assets/media/ — that copy
# is what's committed, not this file).
starsilk header.mp4
.hero_video_work/

# Backup / audit artifacts that may appear during local processing.
*.zip
*.zip.part*
*_before_*.html
audit-temp*
audit-output*
media_vault_fragment.html

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
