<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8"/>
<meta content="width=device-width,initial-scale=1" name="viewport"/>
<meta content="dark" name="color-scheme"/>
<meta name="theme-color" content="{{ config.theme_color }}"/>
<title>{{ config.name }} — Offline</title>
<link rel="stylesheet" href="offline.css"/>
</head>
<body>
<main>
<p class="eyebrow">{{ config.name }} / offline</p>
<h1>The archive is not reachable right now.</h1>
<p>The installed reading shell and public metadata are kept small on purpose. Published images and video are requested only when you open them, so they are unavailable until the network returns.</p>
<p><a href="index.html">Return to the cached Compendium shell</a></p>
</main>
</body>
</html>
